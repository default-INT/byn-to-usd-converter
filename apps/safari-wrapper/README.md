# Safari wrapper (planned)

Xcode project that will embed the WXT-built Safari Web Extension for macOS and iOS.

Not scaffolded yet — Chrome via WXT is the current target.

```
safari-wrapper/
├── BYNConverter.xcodeproj
├── Shared (App)/
├── macOS (Extension)/
└── iOS (Extension)/
```
