/** Match patterns for sites where the content script runs. */
export const SUPPORTED_SITE_MATCHES = [
  "*://re.kufar.by/*",
  "*://realt.by/*",
  "*://www.realt.by/*",
  "*://av.by/*",
  "*://www.av.by/*",
] as const;
